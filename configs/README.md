# Configuration Files

Configuration for slide types and processing parameters.

## Files (to be created)

- `slide_config.yaml` - Per-slide-type configuration
- `markers.yaml` - IHC marker definitions
- `segmentation.yaml` - Segmentation parameters
